'use client'; import {Volume2, Play, Pause} from 'lucide-react'; import {useEffect,useState} from 'react'; import {Exhibit} from '@/lib/types'
export function AudioNarration({exhibit}:{exhibit:Exhibit}){const [playing,setPlaying]=useState(false); useEffect(()=>()=>window.speechSynthesis?.cancel(),[]); function toggle(){if(!('speechSynthesis'in window))return; if(playing){speechSynthesis.cancel();setPlaying(false)}else{const u=new SpeechSynthesisUtterance(`${exhibit.title}. ${exhibit.description}`);u.onend=()=>setPlaying(false);speechSynthesis.speak(u);setPlaying(true)}} return <div className="flex items-center gap-4 rounded-xl bg-[#f1e5d7] p-4"><span className="grid h-10 w-10 place-items-center rounded-full bg-maroon text-white"><Volume2 size={18}/></span><div className="flex-1"><p className="text-sm font-bold">Listen to the curator</p><p className="text-xs text-[#806d60]">Browser narration · about 1 min</p></div><button onClick={toggle} className="focus-ring rounded-full bg-ink p-3 text-white hover:bg-maroon" aria-label={playing?'Pause narration':'Play narration'}>{playing?<Pause size={16}/>:<Play size={16}/>}</button></div>}
