'use client'; import Image from 'next/image'; import {useState} from 'react'; import {Exhibit} from '@/lib/types';
export function MediaSwitch({exhibit}:{exhibit:Exhibit}){const [model,setModel]=useState(false); return <div><div className="relative">{model&&exhibit.embedUrl?<div className="aspect-square overflow-hidden rounded-2xl bg-[#2f211b]"><iframe title={`${exhibit.title} 3D model`} src={exhibit.embedUrl} className="h-full w-full border-0" allow="autoplay; fullscreen; xr-spatial-tracking" allowFullScreen /></div>:<div className="relative aspect-square overflow-hidden rounded-2xl"><Image src={exhibit.image} alt={exhibit.title} fill className="object-cover"/></div>}</div>{exhibit.has3D&&exhibit.embedUrl&&<button onClick={()=>setModel(!model)} className="focus-ring mt-3 w-full rounded-xl border border-maroon py-3 text-sm font-bold text-maroon hover:bg-maroon hover:text-white">{model?'View exhibit image':'View in interactive 3D'}</button>}</div>}
